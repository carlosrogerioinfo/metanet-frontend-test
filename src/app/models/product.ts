export interface ProductResponse {
    id?: string
    barCode?: string
    description?: string
    price?: number
}

export interface Unity {
    id?:            string;
    name?:          string;
}